# Copy this file to .env and fill in your key:
#   cp .env.example .env
TMDB_API_KEY=

# Browser path (vibuxer/hgcloud): set to 1 only on hosts with >= 1GB RAM.
# Leave 0 (default) for the pure-HTTP resolver (free tier / VPS-lite).
BROWSER_ENABLED=0

